# Approved reference-led visual direction

Decision date: 2026-08-24  
Decision: **Selected for LibreRing V1 visual implementation**  
Overall project status: **`DESIGN_AND_SCORING_APPROVED`**

The user confirmed that LibreRing should proceed with this Open Design direction.
It is now the visual source of truth for typography, spacing, colour, card
geometry, motion, and mobile composition.

## Artifacts

- `prototype.html` — 12-screen gallery and focused 390×844 interactive phone.
- `DESIGN.md` — visual tokens and system rules.
- `brand-spec.md` — compact brand contract.
- `design-contract.md` — evidence, boundaries, and acceptance checklist.
- `implementation-handoff.md` — build mapping for the next implementation stage.
- `SOURCE_SHA256SUMS` — import-integrity record for the frozen source files.

The original `prototype/` remains the functional, content, scoring, and edge-case
reference for its 36 routes and eight journeys. Its earlier visual treatment is
superseded; it must not be discarded until every route has been mapped to the
approved system.

## Gate boundary

This artifact was first approved as the visual direction. On 2026-08-24 the user
then supplied an unmistakable equivalent approval of both design and scoring
after the remaining scoring gate was stated explicitly. Production work is
therefore authorised after the required design/model freeze is complete.

## Integrity

The artifact is self-contained and uses project-authored HTML, CSS, JavaScript,
and SVG. It has no external font, image, script, or stylesheet dependency.
