# Approved V1 design export

`librering-approved-v1.html` is the immutable, self-contained visual export
approved on 2026-08-24. Its SHA-256 is:

```text
9a18c099637eaf934b73c2c2d096a628077d8f5a66009e03f0d72d57b180d195
```

The master specification originally named a Penpot backup. After verified Penpot
automation friction, the user explicitly authorised an equivalent design tool
and selected the Open Design result. This HTML export, the machine-readable
tokens, and the design specifications are therefore the canonical equivalent;
no fake `.penpot` file is created.
