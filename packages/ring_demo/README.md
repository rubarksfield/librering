# ring_demo

Deterministic LibreRing sample records for screenshots, tests, and review. Every
record is explicitly marked `DataOrigin.demo`; the package is only injected when
the app is built with `LIBRERING_DEMO=true`.
