name: ring_demo
description: Deterministic, visibly labelled LibreRing demonstration records.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.1

dependencies:
  ring_core:
    path: ../ring_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
