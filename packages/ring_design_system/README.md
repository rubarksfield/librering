# ring_design_system

Frozen V1 colors, spacing, radii, typography, motion, controls, cards, wordmark,
and project-authored vector artwork. This package owns appearance only and does
not interpret health data.
