# ring_ble

Pure-Dart BLE transport, driver, sync-state, retry, and deduplication contracts.
The package has no platform plugin dependency. A later adapter may wrap the
audited `flutter_reactive_ble` package without exposing it to protocol or UI code.
