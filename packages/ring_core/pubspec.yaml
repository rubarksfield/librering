name: ring_core
description: Immutable domain records shared across LibreRing packages.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
